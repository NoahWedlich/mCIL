/*
for (num i = 0; true; i = i+1)
{
	print i;
	if (i >= 5)
	{
		break;
	}
}
*/

def sum(num a, num b) -> num;

def print_sum(num a, num b)
{
	print sum(a, b);
	print "\n";
}


def sum(num a, num b) -> num
{
	return (a + b);
}

print_sum(1, 2);
print_sum(2, 3);
print_sum(2, 5);