/*
for (num i = 0; true; i = i+1)
{
	print i;
	if (i >= 5)
	{
		break;
	}
}
*/

def sum(num a, num b) -> num;

def call_sum(num a, num b)
{
	sum(a, b);
}


def sum(num a, num b) -> num
{
	return (a + b);
}

call_sum(1, 2);
call_sum(2, 3);
call_sum(2, 5);